<% provide(:title, @title) %>
<% unless @parent %>
    <% provide(:subtitle, link_to('subscriptions', subscriptions_path)) %>
    <% provide(:form, render('subscriptions/form')) %>
<% end %>
<%= render @entries %>
