<% provide(:title, 'Known Feeds') %>
<% provide(:form, link_to("↻ all", refresh_all_feeds_path,
		title: 'refresh all active feeds', class: "btn btn-info btn-small")) %>

<% if @feeds.any? %>
    <%= render @feeds %>
<% else %>
    <em>no feeds to view</em>
<% end %>
